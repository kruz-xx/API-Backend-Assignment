# Module 02 — Building Your First API

## 📌 Overview
Setting up a minimal FastAPI application, defining route decorators, handling query parameters, path parameters, and request bodies with Pydantic validation.

---

## 📝 Conceptual Questions & Implementation Notes

### 1. Minimal Health Check Endpoint
> Implement a minimal FastAPI app with a `GET /health` endpoint returning `{"status": "ok"}`.

### 2. Path Parameters vs Query Parameters
> Explain the structural and semantic difference between path parameters (e.g. `/users/{user_id}`) and query parameters (e.g. `/users?role=admin&limit=10`).

**Answer:**
*(Write your explanation here)*

### 3. Request Body & Pydantic Schemas
> How does Pydantic enforce schema validation, type coercion, and automatic generation of OpenAPI specs in FastAPI?

**Answer:**
*(Write your explanation here)*

---

## 📷 Screenshots & Execution Proof

*(Store screenshots in `../screenshots/module_02/` and link them below)*

```markdown
<!-- Example screenshot embed:
![FastAPI Uvicorn Startup](../screenshots/module_02/uvicorn_startup.png)
![Postman Health Endpoint](../screenshots/module_02/postman_health_check.png)
-->
```
